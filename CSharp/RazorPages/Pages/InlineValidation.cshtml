@page
@model RazorPages.Pages.InlineValidation

@{
    ViewData["Title"] = "Inline Validation (ASP.NET Razor Pages)";
}

<div class="demo-section">
    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-page="./InfiniteScroll" class="demo-nav-link">
            ← PREVIOUS: INFINITE SCROLL
        </a>
        <a asp-page="./LazyLoad" class="demo-nav-link">
            NEXT: LAZY LOAD →
        </a>
    </div>

    <section id="example">
        <h2>Inline Validation (ASP.NET Razor Pages)</h2>
        <p>Inline validation demonstration will go here...</p>
        <div class="demo-content" role="region" aria-label="Demo visualization">
            <div style="background: rgba(255, 255, 255, 0.05); padding: 2rem; border-radius: 4px; text-align: center;">
                demo goes here
            </div>
        </div>
    </section>

    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-page="./InfiniteScroll" class="demo-nav-link">
            ← PREVIOUS: INFINITE SCROLL
        </a>
        <a asp-page="./LazyLoad" class="demo-nav-link">
            NEXT: LAZY LOAD →
        </a>
    </div>
</div>

@await Html.PartialAsync("_MoreExamples")