@page
@model RazorPages.Pages.Sortable

@{
    ViewData["Title"] = "Sortable (ASP.NET Razor Pages)";
}

<div class="demo-section">
    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-page="./ProgressBar" class="demo-nav-link">
            ← PREVIOUS: PROGRESS BAR
        </a>
        <a asp-page="./ValueSelect" class="demo-nav-link">
            NEXT: VALUE SELECT →
        </a>
    </div>

    <section id="example">
        <h2>Sortable (ASP.NET Razor Pages)</h2>
        <p>Sortable demonstration will go here...</p>
        <div class="demo-content" role="region" aria-label="Demo visualization">
            <div style="background: rgba(255, 255, 255, 0.05); padding: 2rem; border-radius: 4px; text-align: center;">
                demo goes here
            </div>
        </div>
    </section>

    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-page="./ProgressBar" class="demo-nav-link">
            ← PREVIOUS: PROGRESS BAR
        </a>
        <a asp-page="./ValueSelect" class="demo-nav-link">
            NEXT: VALUE SELECT →
        </a>
    </div>
</div>

@await Html.PartialAsync("_MoreExamples")