@{
    ViewData["Title"] = "Lazy Tabs (ASP.NET MVC)";
}

<div class="demo-section">
    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-controller="Home" asp-action="LazyLoad" class="demo-nav-link">
            ← PREVIOUS: LAZY LOAD
        </a>
        <a asp-controller="Home" asp-action="ProgressBar" class="demo-nav-link">
            NEXT: PROGRESS BAR →
        </a>
    </div>

    <section id="example">
        <h2>Lazy Tabs (ASP.NET MVC)</h2>
        <p>Lazy tabs demonstration will go here...</p>
        <div class="demo-content" role="region" aria-label="Demo visualization">
            demo goes here
        </div>
    </section>

    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-controller="Home" asp-action="LazyLoad" class="demo-nav-link">
            ← PREVIOUS: LAZY LOAD
        </a>
        <a asp-controller="Home" asp-action="ProgressBar" class="demo-nav-link">
            NEXT: PROGRESS BAR →
        </a>
    </div>
</div>

@await Html.PartialAsync("_MoreExamples")