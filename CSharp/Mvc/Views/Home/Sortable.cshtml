@{
    ViewData["Title"] = "Sortable (ASP.NET MVC)";
}

<div class="demo-section">
    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-controller="Home" asp-action="ProgressBar" class="demo-nav-link">
            ← PREVIOUS: PROGRESS BAR
        </a>
        <a asp-controller="Home" asp-action="ValueSelect" class="demo-nav-link">
            NEXT: VALUE SELECT →
        </a>
    </div>

    <section id="example">
        <h2>Sortable (ASP.NET MVC)</h2>
        <p>Sortable demonstration will go here...</p>
        <div class="demo-content" role="region" aria-label="Demo visualization">
            demo goes here
        </div>
    </section>

    <div style="display: flex; justify-content: space-between; gap: 1rem;">
        <a asp-controller="Home" asp-action="ProgressBar" class="demo-nav-link">
            ← PREVIOUS: PROGRESS BAR
        </a>
        <a asp-controller="Home" asp-action="ValueSelect" class="demo-nav-link">
            NEXT: VALUE SELECT →
        </a>
    </div>
</div>

@await Html.PartialAsync("_MoreExamples")